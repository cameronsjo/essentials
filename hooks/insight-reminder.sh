#!/bin/bash
# PreCompact nudge: remind to capture insights before context is lost.
# Lightweight — just outputs a reminder, never blocks.

echo "Context is about to be compacted. If there are any unsaved insights or gotchas from this session, run /field-notes to capture them before they're lost."
exit 0
