# Essentials — End-of-Session Steps

- **Workbench update**: If new skills, commands, or hooks were added, check if `workbench/marketplace.json` needs a new entry or description update
- **Push conflicts**: Remote rejections on push are typically auto-bump version commits from GitHub Actions. Pull with `--no-rebase` and push again
