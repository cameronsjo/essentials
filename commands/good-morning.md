---
description: Start-of-day orientation -- git status, memory context, recent activity across all repos
allowed-tools: Bash(git *), Bash(pgrep *), Bash(bd *), Read, Glob
---

Run the `good-morning` skill. Read `skills/good-morning/SKILL.md` and follow it exactly.
