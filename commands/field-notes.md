---
description: Capture or review session insights and gotchas
allowed-tools: Read, Write, Edit, Bash(git *), Glob, Grep, AskUserQuestion
---

Run the `field-notes` skill. Read `skills/field-notes/SKILL.md` and follow it exactly.

Arguments:
- (none) or `capture` — capture mode (default)
- `review` — review and grade existing entries
