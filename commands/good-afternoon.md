---
description: Re-orient after /clear or compaction -- reload uncommitted changes into context
allowed-tools: Bash(git *), Read, Glob
---

Run the `good-afternoon` skill. Read `skills/good-afternoon/SKILL.md` and follow it exactly.
